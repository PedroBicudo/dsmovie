import styled from "styled-components";

const MovieStarsContainer = styled.div`
    width: 130px;
`;

const StarWrapper = styled.div`
    svg {
        width: 22px;
        height: auto;
    }
`;

export {
    MovieStarsContainer,
    StarWrapper
}